---
target: dashboard (web/src/App.tsx)
total_score: 33
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 2
timestamp: 2026-08-31T19-01-37Z
slug: web-src-app-tsx
---
# Design Critique v2: Baltic OSINT Hub dashboard (web/src/App.tsx)

Method: dual-agent (A: design review with live screenshots of :8080 · B: detector + runtime evidence). Live posture during review: High (4 of 5). Detector degraded (regex mode); runtime clean (0 errors, 0 exceptions, root mounted — WebGL crash fix confirmed). Detector defect noted: ignoreValues not applied in degraded mode.

## Design Health Score — 33/40 (Good) · up from 31

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 3 | Filter changes leave stale rows with no busy indicator until refetch |
| 2 | Match System / Real World | 3 | Bare "S4" chips on tile headlines; "Hollow" needs collapsed disclosure |
| 3 | User Control and Freedom | 3 | Back never undoes a drill-through (history.replaceState) |
| 4 | Consistency and Standards | 3 | status-serious #ec835a vs sabotage #eb6834 near-identical hues |
| 5 | Error Prevention | 4 | URL validation, NaN-proofing, constrained inputs |
| 6 | Recognition Rather Than Recall | 3 | Some definitions only in collapsed disclosures / hover titles |
| 7 | Flexibility and Efficiency | 3 | No keyboard path to chart interactions |
| 8 | Aesthetic and Minimalist Design | 3 | Feed metadata rows carry up to 7 elements |
| 9 | Error Recovery | 4 | Plain-language failed-part card; WebGL fallback names cause + where data lives |
| 10 | Help and Documentation | 4 | Four disclosures, one grammar |
| Total | | 33/40 | Good |

All three P1s from the 2026-08-31 baseline critique (contrast, false-calm load, missing readiness path) are resolved; heuristics 5 and 9 rose to 4.

## Design Specificity Verdict

Deeply product-grounded; Control Room Wall executes faithfully in both themes (flat field, fixed slots, unthemed status hues with AA text variants working). Detector: 2 findings, both previously adjudicated side-tab exceptions (chip spine, state-media marker); tool does not apply config ignoreValues in degraded mode. Runtime: ROOT CHILDREN 1, no console errors.

## Cognitive Load — 2 failures (moderate; inherent density, well managed)

Fails: chunking (feed row 5–7 chips; SAR table 8 columns) and minimal choices (sidenav 7; filter strip 6 controls/11-option select; tile up to 7 interactive elements; SAR ~8 controls; timeline legend up to 9 keys). Passes the other six.

## Priority Issues

- [P1] "Incidents per day" counts S1 commentary as incidents — op-eds and interviews stack the trend bars and tile counts against the "events, not articles" claim. Fix: exclude severity-1/Analysis from chart and tile counts by default (or muted analysis band); filter re-includes. → distill + API defaults
- [P1] At High with no baseline, "is this week unusual?" is answered "can't say" in the smallest type (posture-context). Fix: when typical_week <= 0 and level >= 3, promote calibration into the reading and state which rule would lower it. → clarify
- [P2] Timeline mouse-only: no keyboard/AT path to bar-click or brush, no table alternative. Fix: hidden per-day table or focusable day list mirroring onSelectDay. → adapt
- [P2] status-serious and sabotage share a hue; level-4 ladder wears sabotage orange. Fix: shift --status-serious to distinct salmon/brown; update DESIGN.md. → colorize
- [P2] State media can hold the feed's first row (RIA led during review). Fix: within day groups, sort state-media after independent/institutional. → polish

## Persona Red Flags

- Jordan: bare S4 chips hover-only; "Unconfirmed" defined only in disclosure; "Hollow" opaque.
- Sam: timeline inoperable; 48 identical "imagery" links; "▼ 6" announced as glyph. Credits: skip link, aria-live count, focus-following drill-throughs.
- Casey: sidenav pills ~26px at mobile and MISSED by the 44px coarse-pointer rule; tile drill rows ~19px targets.
- Rūta: High + "can't say if typical" in smallest type; satellite "change detected" reads as second alarm with defusal below 48 rows; RIA leads feed. Saved by balance bar, prepare link, level voice.

## Minor Observations

- Day chip shows raw ISO date; feed headers show "Thu, 27 Aug 2026".
- .banner-issue:hover uses raw --status-warning as decoration (system's own Don'ts forbid).
- Chevron rotation not reduced-motion-gated.
- Corroboration source list hover-title-only.
- severityTextColor hardcodes hex instead of tokens.
- Stale rows could dim during refetch.

## Questions to Consider

1. Should one corroborated intercept be "High"? Does the rule table need a pattern-vs-novel dimension?
2. What does "incident" mean — should public counts gate to severity >= 2 so numbers only mean things that happened?
3. Is chronology the right sovereign for an adversary-aware product's feed order?
